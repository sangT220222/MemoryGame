import React from "react";

const Card = (props) => {
  const { wasClicked, setWasClicked } = useState(false);

  const handleClick = () => {
    setWasClicked(!wasClicked);
    if (wasClicked) {
        props.
    }
  };
  return;
};

export default Card;
